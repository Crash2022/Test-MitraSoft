// import Spinner from 'react-bootstrap/Spinner';
//
// export const useSpinner = (status: string) => {
//     if (status === 'loading') return <Spinner animation="border" variant="primary" style={{marginTop: '300px'}}/>
// }

export default {}